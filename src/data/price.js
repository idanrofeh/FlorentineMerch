export const priceData = {
    getProductPrice
}

function getProductPrice(print, itemType, amount) {
    let priceForOne;
    // switch (itemType) {
    //     case "short":

    //             break;
    // }
}